#!/usr/bin/node

let myArgs = process.argv;
if (myArgs.length === 2) {
    console.log('No argument');
}
el
se
if (myArgs.length === 3) {
    console.log('Argument found');
} else {
    console.log('Arguments found');
}